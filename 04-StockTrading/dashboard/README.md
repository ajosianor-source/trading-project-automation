# Dashboard

The dashboard is a dependency-free static console. The scanner writes
`scan-data.js`, allowing the page to work when opened directly from disk without
a local web server.

If the provider label says `SYNTHETIC DEMO`, every displayed value is generated
test data and has no market meaning.
