[project]
name = "healthgov-plugin-sdk"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11,<3"]

[build-system]
requires = ["setuptools>=78"]
build-backend = "setuptools.build_meta"

